worker_processes auto;

events {
  worker_connections 1024;
}

http {
  server {
    listen 80;
    root  /usr/share/nginx/data;
    index index.html;
    include /etc/nginx/mime.types;

    location / {
    }
  }
}
